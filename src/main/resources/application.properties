spring.application.name=auth 

#spring.datasource.url=jdbc:mysql://localhost:3306/auth_db
spring.datasource.url=jdbc:mysql://0.tcp.eu.ngrok.io:18547/auth_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Nouronino@21
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Secret Key
jwt.secret=mySecretKeyForJWTTokenGenerationAndValidation123456789
# 1 minutes
jwt.expiration=60000

# 15 minutes
#jwt.expiration=900000

# 1 hour
#jwt.expiration=3600000

# 24 hours (your setting)
#jwt.expiration=86400000

# 7 days
#jwt.expiration=604800000

# 30 days
#jwt.expiration=2592000000

server.port=1001

springdoc.swagger-ui.path=/swagger-ui.html


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mm3138242@gmail.com
spring.mail.password=ubyrswgnkmwyrvud
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Optional but recommended
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
spring.mail.default-encoding=UTF-8
# spring.mail.test-connection=true   # uncomment to fail fast if SMTP unreachable
